CREATE DATABASE IF NOT EXISTS travel_agency_db;
USE kursova;


CREATE TABLE Roles (
    role_id INT AUTO_INCREMENT PRIMARY KEY,
    role_name VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE Users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    phone_number VARCHAR(20),
    role_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (role_id) REFERENCES Roles(role_id) 
        ON DELETE SET NULL ON UPDATE CASCADE
);

CREATE TABLE Regions (
    region_id INT AUTO_INCREMENT PRIMARY KEY,
    region_name VARCHAR(100) NOT NULL UNIQUE
);

CREATE TABLE Tours (
    tour_id INT AUTO_INCREMENT PRIMARY KEY,
    region_id INT,
    tour_title VARCHAR(255) NOT NULL,
    description TEXT,
    price_per_person DECIMAL(10, 2) NOT NULL,
    start_date DATE,
    end_date DATE,
    image_path VARCHAR(255), 
    is_active BOOLEAN DEFAULT TRUE,
    FOREIGN KEY (region_id) REFERENCES Regions(region_id)
        ON DELETE CASCADE ON UPDATE CASCADE
);

CREATE TABLE Bookings (
    booking_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    tour_id INT,
    booking_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(50) DEFAULT 'Очікує підтвердження',
    total_amount DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (tour_id) REFERENCES Tours(tour_id)
        ON DELETE CASCADE ON UPDATE CASCADE
);

CREATE TABLE Payments (
    payment_id INT AUTO_INCREMENT PRIMARY KEY,
    booking_id INT,
    payment_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    amount DECIMAL(10, 2) NOT NULL,
    payment_method VARCHAR(50), 
    FOREIGN KEY (booking_id) REFERENCES Bookings(booking_id)
        ON DELETE CASCADE ON UPDATE CASCADE
);

CREATE TABLE Reviews (
    review_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    tour_id INT,
    rating INT CHECK (rating BETWEEN 1 AND 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (tour_id) REFERENCES Tours(tour_id)
        ON DELETE CASCADE ON UPDATE CASCADE
);

INSERT INTO Roles (role_name) VALUES ('Адміністратор'), ('Клієнт');

INSERT INTO Regions (region_name) VALUES ('Європа'), ('Азія'), ('Америка');

INSERT INTO Tours (region_id, tour_title, description, price_per_person, start_date, end_date) 
VALUES 
(1, 'Відпустка в Парижі', '5 днів у романтичному місті', 1200.00, '2026-06-01', '2026-06-06'),
(2, 'Скарби Токіо', 'Екскурсійний тур Японією', 2500.00, '2026-07-10', '2026-07-20');
